# Install instructions

## Install Docker

Run the script at ``

Try to run the `hello-world` container:

```bash
docker run hello-world
```

## Start the container

Run the command

```bash
make docker-up
```
